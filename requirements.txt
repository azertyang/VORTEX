# Pinned versions known to work with Nicheformer (Python 3.9+).
# Install order matters: install torch first, then the rest.

# --- Deep learning core ---
torch>=2.1,<2.4
pytorch-lightning>=2.0,<2.3

# --- Single-cell / spatial omics ---
anndata>=0.10
scanpy>=1.9
squidpy>=1.4
scikit-learn>=1.3
numba>=0.58
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10
h5py>=3.10

# --- Used by Nicheformer's _fine_tune_model.py ---
pyro-ppl>=1.8

# --- Visualisation / utilities ---
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
PyYAML>=6.0
umap-learn>=0.5

# --- Reproducibility ---
einops>=0.7
